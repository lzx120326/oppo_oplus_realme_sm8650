--- a/fs/proc/task_mmu.c	2024-12-17 11:21:16.646581300 -0500
+++ b/fs/proc/task_mmu.c	2024-12-17 11:35:36.873887048 -0500
@@ -416,6 +416,23 @@
 extern void susfs_sus_ino_for_show_map_vma(unsigned long ino, dev_t *out_dev, unsigned long *out_ino);
 #endif
 
+static void show_vma_header_prefix_fake(struct seq_file *m,
+					unsigned long start, unsigned long end,
+					vm_flags_t flags, unsigned long long pgoff,
+					dev_t dev, unsigned long ino)
+{
+	seq_setwidth(m, 25 + sizeof(void *) * 6 - 1);
+	seq_printf(m, "%08lx-%08lx %c%c%c%c %08llx %02x:%02x %lu ",
+			start,
+			end,
+			flags & VM_READ ? 'r' : '-',
+			flags & VM_WRITE ? 'w' : '-',
+			flags & VM_EXEC ? '-' : '-',
+			flags & VM_MAYSHARE ? 's' : 'p',
+			pgoff,
+			MAJOR(dev), MINOR(dev), ino);
+}
+
 static void
 show_map_vma(struct seq_file *m, struct vm_area_struct *vma)
 {
@@ -427,6 +444,7 @@
 	unsigned long start, end;
 	dev_t dev = 0;
 	const char *name = NULL;
+	struct dentry *dentry;
 
 	if (file) {
 		struct inode *inode = file_inode(vma->vm_file);
@@ -442,6 +460,23 @@
 bypass_orig_flow:
 #endif
 		pgoff = ((loff_t)vma->vm_pgoff) << PAGE_SHIFT;
+		dentry = file->f_path.dentry;
+        if (dentry) {
+        	const char *path = (const char *)dentry->d_name.name; 
+            if (strstr(path, "lineage")) {
+			start = vma->vm_start;
+			end = vma->vm_end;
+			show_vma_header_prefix(m, start, end, flags, pgoff, dev, ino);
+			name = "/system/framework/framework-res.apk";
+			goto done;
+            }
+			if (strstr(path, "jit-zygote-cache")) { 
+			start = vma->vm_start;
+			end = vma->vm_end;
+			show_vma_header_prefix_fake(m, start, end, flags, pgoff, dev, ino);
+			goto bypass;
+            }
+        }
 	}
 
 	start = vma->vm_start;
@@ -449,6 +484,7 @@
 	if (show_vma_header_prefix(m, start, end, flags, pgoff, dev, ino))
 		return;
 
+	bypass:
 	/*
 	 * Print the dentry name for named mappings, and a
 	 * special [heap] marker for the heap:
--- a/fs/proc/base.c	2024-12-15 11:30:00.213422100 -0500
+++ b/fs/proc/base.c	2024-12-15 11:36:21.422813925 -0500
@@ -2229,11 +2229,17 @@
 
 	rc = -ENOENT;
 	vma = find_exact_vma(mm, vm_start, vm_end);
-	if (vma && vma->vm_file) {
-		*path = vma->vm_file->f_path;
-		path_get(path);
-		rc = 0;
-	}
+	if (vma) {
+        if (vma->vm_file) {
+            if (strstr(vma->vm_file->f_path.dentry->d_name.name, "lineage")) { 
+				rc = kern_path("/system/framework/framework-res.apk", LOOKUP_FOLLOW, path);
+			} else {
+				*path = vma->vm_file->f_path;
+				path_get(path);
+				rc = 0;
+            }
+        }
+    }
 	mmap_read_unlock(mm);
 
 out_mmput:
 

diff --git a/fs/proc/spoof_version.c b/fs/proc/spoof_version.c
new file mode 100644
--- /dev/null
+++ b/fs/proc/spoof_version.c
@@ -0,0 +1,51 @@
+/*
+ * spoof_version.c - /proc/version 伪装（内置版, 重试机制）
+ */
+#include <linux/kernel.h>
+#include <linux/init.h>
+#include <linux/proc_fs.h>
+#include <linux/fs.h>
+#include <linux/uaccess.h>
+#include <linux/workqueue.h>
+#include <linux/sched.h>
+
+static const char fake_version[] =
+"Linux version 6.1.141-android14-11-o-g16e6ef702577 (build@kvm-slave) (clang version 17.0.2) #1 SMP PREEMPT Tue Apr 14 16:00:22 CST 2026\n";
+
+static ssize_t spoof_version_read(struct file *file, char __user *buf, size_t count, loff_t *ppos)
+{
+	return simple_read_from_buffer(buf, count, ppos, fake_version, sizeof(fake_version) - 1);
+}
+
+static const struct proc_ops spoof_version_ops = {
+	.proc_read = spoof_version_read,
+	.proc_lseek = default_llseek,
+};
+
+static struct delayed_work spoof_version_work;
+
+static void spoof_version_do_work(struct work_struct *w)
+{
+	int i;
+
+	for (i = 0; i < 6; i++) {
+		remove_proc_entry("version", NULL);
+		if (proc_create("version", 0444, NULL, &spoof_version_ops)) {
+			pr_info("spoof: version hijacked\n");
+			return;
+		}
+		pr_info("spoof: version retry %d\n", i + 1);
+		schedule_delayed_work(&spoof_version_work, msecs_to_jiffies(30000));
+		return;
+	}
+	pr_err("spoof: version give up\n");
+}
+
+static int __init spoof_version_init(void)
+{
+	INIT_DELAYED_WORK(&spoof_version_work, spoof_version_do_work);
+	schedule_delayed_work(&spoof_version_work, msecs_to_jiffies(20000));
+	return 0;
+}
+
+late_initcall(spoof_version_init);

diff --git a/fs/proc/spoof_cpuinfo.c b/fs/proc/spoof_cpuinfo.c
new file mode 100644
--- /dev/null
+++ b/fs/proc/spoof_cpuinfo.c
@@ -0,0 +1,133 @@
+/*
+ * spoof_cpuinfo.c - /proc/cpuinfo 伪装 (Kirin 9030S 8核真机数据)
+ */
+#include <linux/kernel.h>
+#include <linux/init.h>
+#include <linux/proc_fs.h>
+#include <linux/fs.h>
+#include <linux/uaccess.h>
+#include <linux/workqueue.h>
+#include <linux/sched.h>
+
+static const char fake_cpuinfo[] =
+"Processor	: AArch64 Processor rev 0 (aarch64)\n"
+"processor	: 0\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd24\n"
+"CPU revision	: 0\n"
+"CPU physical	: 0\n"
+"\n"
+"processor	: 1\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd24\n"
+"CPU revision	: 0\n"
+"CPU physical	: 1\n"
+"\n"
+"processor	: 2\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd24\n"
+"CPU revision	: 0\n"
+"CPU physical	: 2\n"
+"\n"
+"processor	: 3\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd24\n"
+"CPU revision	: 0\n"
+"CPU physical	: 3\n"
+"\n"
+"processor	: 4\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd47\n"
+"CPU revision	: 0\n"
+"CPU physical	: 4\n"
+"\n"
+"processor	: 5\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd47\n"
+"CPU revision	: 0\n"
+"CPU physical	: 5\n"
+"\n"
+"processor	: 6\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd47\n"
+"CPU revision	: 0\n"
+"CPU physical	: 6\n"
+"\n"
+"processor	: 7\n"
+"BogoMIPS	: 26.00\n"
+"Features	: fp asimd evtstrm aes pmull sha1 sha2 crc32 atomics fphp asimdhp cpuid asimdrdm jscvt fcma lrcpc dcpop sha3 sm3 sm4 asimddp sha512 sve asimdfhm dit uscat ilrcpc flagm ssbs sb paca pacg dcpodp flagm2 frint svei8mm svebf16 i8mm bf16 dgh bti\n"
+"CPU implementer	: 0x48\n"
+"CPU architecture: 8\n"
+"CPU variant	: 0x2\n"
+"CPU part	: 0xd06\n"
+"CPU revision	: 0\n"
+"CPU physical	: 7\n"
+"\n"
+"Hardware	: HUAWEI Kirin9030S\n"
+;
+
+static ssize_t spoof_read(struct file *file, char __user *buf, size_t count, loff_t *ppos)
+{
+\treturn simple_read_from_buffer(buf, count, ppos, fake_cpuinfo, sizeof(fake_cpuinfo) - 1);
+}
+
+static const struct proc_ops spoof_proc_ops = {
+\t.proc_read = spoof_read,
+\t.proc_lseek = default_llseek,
+};
+
+static struct delayed_work spoof_cpuinfo_work;
+
+static void spoof_cpuinfo_do_work(struct work_struct *w)
+{
+\tint i;
+
+\tfor (i = 0; i < 6; i++) {
+\t\tremove_proc_entry("cpuinfo", NULL);
+\t\tif (proc_create("cpuinfo", 0444, NULL, &spoof_proc_ops)) {
+\t\t\tpr_info("spoof: cpuinfo hijacked\n");
+\t\t\treturn;
+\t\t}
+\t\tpr_info("spoof: cpuinfo retry %d\n", i + 1);
+\t\tschedule_delayed_work(&spoof_cpuinfo_work, msecs_to_jiffies(30000));
+\t\treturn;
+\t}
+\tpr_err("spoof: cpuinfo give up\n");
+}
+
+static int __init spoof_cpuinfo_init(void)
+{
+\tINIT_DELAYED_WORK(&spoof_cpuinfo_work, spoof_cpuinfo_do_work);
+\tschedule_delayed_work(&spoof_cpuinfo_work, msecs_to_jiffies(20000));
+\treturn 0;
+}
+
+late_initcall(spoof_cpuinfo_init);

diff --git a/fs/proc/Makefile b/fs/proc/Makefile
--- a/fs/proc/Makefile
+++ b/fs/proc/Makefile
@@ -1,6 +1,7 @@
 # SPDX-License-Identifier: GPL-2.0
 #
 # Makefile for the Linux proc filesystem routines.
 #
 
+obj-y   += spoof_cpuinfo.o spoof_version.o
 obj-y   += proc.o
